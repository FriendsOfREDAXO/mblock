<?php

// Team-Member Beispiel mit MForm und Namespace

use FriendsOfRedaxo\MForm;
use FriendsOfRedaxo\MBlock\MBlock;

// base ID
$id = 1;

// init mform mit moderner MForm Syntax
$mform = MForm::factory();

// fieldset
$mform->addFieldsetArea('Team member');

// textinput
$mform->addTextField("$id.0.name", array('label'=>'Name')); // use string for x.0 json values

// Media-ID 1 für ersten MBlock
$mform->addMediaField(1, array('label'=>'Avatar')); 

// position field
$mform->addTextField("$id.0.position", array('label'=>'Position'));

// email field  
$mform->addTextField("$id.0.email", array('label'=>'E-Mail'));

// Online/Offline Status (hidden field für Toggle-Funktion)
$mform->addHiddenField("$id.0.mblock_offline", '0');

// MBlock anzeigen mit Namespace
echo MBlock::show($id, $mform->show(), array(
    'min'=>2,
    'max'=>4
));

?>
