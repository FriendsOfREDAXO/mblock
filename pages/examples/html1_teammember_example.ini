<?php

// 💡 HTML-Beispiel für MBlock (Legacy-Support)
// 🚀 EMPFEHLUNG: MForm 8 für moderne, wartbare Module!

// base ID
$id = 1;

// ⚠️ HINWEIS: Rohes HTML ist schwer wartbar und fehleranfällig
// Für Produktionsumgebungen wird MForm 8 empfohlen!

// html form (Legacy-Methode)
$form = <<<EOT
    <fieldset class="form-horizontal">
        <legend>Team member (HTML Legacy)</legend>
        
        <!-- Name Field -->
        <div class="form-group">
            <div class="col-sm-2 control-label">
                <label for="rv2_1_0_name">Name</label>
            </div>
            <div class="col-sm-10">
                <input id="rv2_1_0_name" type="text" 
                       name="REX_INPUT_VALUE[$id][0][name]" 
                       value="" class="form-control">
            </div>
        </div>
        
        <!-- Avatar Field -->
        <div class="form-group">
            <div class="col-sm-2 control-label">
                <label>Avatar</label>
            </div>
            <div class="col-sm-10">
                REX_MEDIA[id="1" widget="1"]
            </div>
        </div>
        
        <!-- Online/Offline Hidden Field -->
        <input type="hidden" name="REX_INPUT_VALUE[$id][0][mblock_offline]" value="0">
        
        <!-- MForm 8 Alternative Hinweis -->
        <div class="alert alert-info">
            <strong>💡 Tipp:</strong> Verwenden Sie <strong>MForm 8</strong> für:
            <ul>
                <li>✅ Typsichere Felder</li>
                <li>✅ Automatische Validierung</li> 
                <li>✅ Bessere Wartbarkeit</li>
                <li>✅ Moderne Syntax</li>
            </ul>
            <code>use FriendsOfRedaxo\MForm; \$mform = MForm::factory();</code>
        </div>
    </fieldset>
EOT;

// parse form
echo MBlock::show($id, $form);

?>