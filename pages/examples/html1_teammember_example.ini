<?php

// HTML-Beispiel für MBlock
// Alternative für Projekte ohne MForm

// base ID
$id = 1;

// html form
$form = <<<EOT
    <fieldset class="form-horizontal">
        <legend>Team member</legend>
        
        <!-- Name Field -->
        <div class="form-group">
            <div class="col-sm-2 control-label">
                <label for="rv2_1_0_name">Name</label>
            </div>
            <div class="col-sm-10">
                <input id="rv2_1_0_name" type="text" 
                       name="REX_INPUT_VALUE[$id][0][name]" 
                       value="" class="form-control">
            </div>
        </div>
        
        <!-- Avatar Field -->
        <div class="form-group">
            <div class="col-sm-2 control-label">
                <label>Avatar</label>
            </div>
            <div class="col-sm-10">
                REX_MEDIA[id="1" widget="1"]
            </div>
        </div>
        
        <!-- Online/Offline Hidden Field -->
        <input type="hidden" name="REX_INPUT_VALUE[$id][0][mblock_offline]" value="0">
    </fieldset>
EOT;

// parse form
echo MBlock::show($id, $form);

?>