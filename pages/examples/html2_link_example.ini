<?php

// 💡 HTML-Beispiel für Links (Legacy-Support)
// 🚀 EMPFEHLUNG: Verwenden Sie MForm 8 für moderne, wartbare Module!

// base ID
$id = 1;

// html form (Legacy-Methode)
$form = <<<EOT
    <fieldset class="form-horizontal">
        <legend>Links (HTML Legacy)</legend>
        
        <!-- Link Field -->
        <div class="form-group">
            <div class="col-sm-2 control-label">
                <label>Link</label>
            </div>
            <div class="col-sm-10">
                REX_LINK[id="1" widget="1"]
            </div>
        </div>
        
        <!-- Linklist Field -->
        <div class="form-group">
            <div class="col-sm-2 control-label">
                <label>Link list</label>
            </div>
            <div class="col-sm-10">
                REX_LINKLIST[id="1" widget="1"]
            </div>
        </div>
        
        <!-- Online/Offline Hidden Field -->
        <input type="hidden" name="REX_INPUT_VALUE[$id][0][mblock_offline]" value="0">
        
        <!-- MForm 8 Alternative -->
        <div class="alert alert-success">
            <strong>🎯 MForm 8 Alternative:</strong><br>
            <code>
            \$mform->addLinkField(1, ['label'=>'Link']);<br>
            \$mform->addLinklistField(1, ['label'=>'Link list']);
            </code>
        </div>
    </fieldset>
EOT;

// parse form
echo MBlock::show($id, $form);

?>