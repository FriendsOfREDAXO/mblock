<?php

// 💡 HTML-Beispiel für Media (Legacy-Support)
// 🚀 EMPFEHLUNG: Verwenden Sie MForm 8 für moderne, wartbare Module!

// base ID
$id = 1;

// html form (Legacy-Methode)
$form = <<<EOT
    <fieldset class="form-horizontal">
        <legend>Media (HTML Legacy)</legend>
        
        <!-- Media Field -->
        <div class="form-group">
            <div class="col-sm-2 control-label">
                <label>Media</label>
            </div>
            <div class="col-sm-10">
                REX_MEDIA[id="1" widget="1"]
            </div>
        </div>
        
        <!-- Medialist Field -->
        <div class="form-group">
            <div class="col-sm-2 control-label">
                <label>Media list</label>
            </div>
            <div class="col-sm-10">
                REX_MEDIALIST[id="2" widget="1"]
            </div>
        </div>
        
        <!-- Online/Offline Hidden Field -->
        <input type="hidden" name="REX_INPUT_VALUE[$id][0][mblock_offline]" value="0">
        
        <!-- Media-ID Warnung -->
        <div class="alert alert-warning">
            <strong>⚠️ Wichtig:</strong> Media-IDs müssen eindeutig sein!<br>
            <strong>Problem:</strong> Bei mehreren MBlocks mit gleichen IDs bleibt die Medialist leer.
        </div>
        
        <!-- MForm 8 Alternative -->
        <div class="alert alert-success">
            <strong>🎯 MForm 8 Alternative:</strong><br>
            <code>
            \$mform->addMediaField(1, ['label'=>'Media']);<br>
            \$mform->addMedialistField(2, ['label'=>'Media list']);
            </code>
        </div>
    </fieldset>
EOT;

// parse form
echo MBlock::show($id, $form);

?>